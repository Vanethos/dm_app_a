name: app_a
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # To only upgrade this dependency, we can use `flutter pub upgrade dependency_manager`
  dependency_manager:
    git:
      url: https://github.com/Vanethos/dm_dependency_manager.git
      ref: master

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
